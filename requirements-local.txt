# Install these when using INFERENCE_BACKEND=local
# Runs the model on your machine (MPS on Apple Silicon, CUDA on NVIDIA, or CPU)
#
# Usage:
#   pip install -r requirements.txt
#   pip install -r requirements-local.txt

torch==2.6.0
transformers==4.51.3
